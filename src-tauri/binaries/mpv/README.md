# Runtime libmpv directory

Windows packaging workflows place the checksum-verified `libmpv-2.dll` and its provenance record in this directory before the Tauri build. The binary itself is intentionally not committed.
